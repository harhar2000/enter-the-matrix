### Unit Tests


# Test Slow Printing of Text: Verify `print_slowly`function prints text one character at a time with the specified delay.

# Test Terminal Width Detection: Ensure `get_terminal_width` function accurately detects terminal width or correctly falls back to the default value when the terminal width cannot be determined.

# Test Name Capture: Confirm script correctly uses name 

# Test Text Wrapping and Formatting: Verify that paragraphs are correctly wrapped according to the terminal width and intentional newlines in the `link_welcome` message are preserved.


### Integration Tests

# Test Complete Flow with Various Terminal Sizes

# Test Delay Parameters Effect: Confirm changing the `custom_delay` parameter affects output speed


### Edge Case Test

# Test with Empty User Name: Check how script handles empty string as input for user's name.